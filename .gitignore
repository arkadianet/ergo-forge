# Implementation plans are not committed. They are execution scaffolding: stale
# the moment they are executed, and they embed code snippets that can be wrong
# (the P3a plan carried a lint bug that the implementation copied verbatim).
# Design rationale IS committed — see docs/superpowers/specs/.
docs/superpowers/plans/

# The ergo-explorer MCP server writes its logs into the working directory.
logs/

# Isolated build output for parallel agent worktrees.
/target-*/

# Third-party contract clones fetched for ingestion measurement — never commit
.lithos-src/
# Browser-review scratch: logs and screenshots, regenerated by tests/positions-browser.mjs
ui/positions-review/
