# Consolidation verification — 2026-09-10 UTC

Run `python3 docs/discovery/consolidation-verification/verify.py` from the
repository root. It sets `CARGO_TARGET_DIR=./target-p00`, records each command's
actual combined output and exit, and hashes the logs in
[verification.json](verification.json).

| Check | Actual result |
|---|---|
| `cargo fmt --all -- --check` | 0 |
| `cargo clippy --workspace --all-targets -- -D warnings` | 0 |
| Python gate tests (`unittest discover -s scripts -p 'test_*.py'`) | 0; 20 tests passed |
| `cargo test --workspace --release` | 0; 499 passed, 0 failed, 1 existing external-only ignored |
| `roadmap_gate.py --through-completed` | 0; passing prefix through D02 |
| `roadmap_gate.py --ci` | 0; M05 and D03 remain recorded stops |

[Example smoke output](example-smoke.json) records actual rejection of empty
export, malformed count, duplicate row, missing probe argument, and success on
one valid row. These checks do not change the workspace test denominator.

[Preservation audit](preservation.json), generated by
`python3 docs/discovery/consolidation-verification/preservation.py`, compares
every tracked starting file except the explicitly edited README and historical
design's appended decision link. Policy, stop records, M00 manifest, source/node
pins, all fixtures, expected facts, caps, property evaluator and tests remain
byte-identical. `.git` was only read. New files are not authenticated by that
prefix audit; their behavior and evidence are checked separately.

The arithmetic entry assessment's separate gate exits 1 because only 3 guarantees
across 2 families qualify. That is the retained negative assessment result, not
a workspace test failure or a resolution of D03/M05. Scanner reruns both exit 0;
acquisition provenance remains incomplete as documented in the census report.
