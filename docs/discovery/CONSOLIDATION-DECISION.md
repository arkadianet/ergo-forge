# Governing consolidation decision — 2026-09-10 UTC

Retain the following capability and positioning:

> An offline contract experiment and regression workbench. It checks supplied
> transactions and finite traces against declared properties using the pinned
> node validator, under explicit state assumptions. Independent semantic review,
> transfer usefulness, and new vulnerability discovery remain unestablished.

**Cancel lifecycle construction and numeric search for this arc.** Solver,
lifecycle and mapping expansions remain closed. M05 recovery, steering, new
mutation axes, larger caps, additional mapping breadth, browser migration,
dashboards and recipes remain displaced. No live acquisition, protocol contact,
broadcast or publication was performed or authorized by this unit.

Explicit registration decision: **leave D04 unregistered**. This record is the
author-authorized consolidation and cancellation decision, not a passing D04
checkpoint or a replacement for D03's failed measurement. The original proposed
D04 target and gates remain visible in the historical design. This decision
supersedes its expectation of future scheduling; it does not change its gates,
claim it was attempted, or resolve a stop. No registry changes are needed to
cancel unregistered work. No fictitious command exit or stop record is added.

Preserve `completedThrough: D02`, D03 `implemented: false`, the M05 and D03 stop
records (and historical D00 stop/resolution), and all first-attempt evidence.
`discoveryCredit: 0`. Independent semantic measurement remains incomplete;
transfer denominator and usefulness remain null. Synthetic agreement and syntax
acceptance do not establish meaning, useful transfer or a new vulnerability.
M00's original anchor remains byte-identical:
`41729240759244bd3edc833e31359bfe50d8430f0b5a2e27ebc727706363cd10`.

A single arithmetic **entry assessment**, capped at two person-days, was allowed.
Its [20 frozen candidates and report](arithmetic-assessment/REPORT.md) yielded
3 qualifying guarantees across 2 families, below the required 6 across 3. The
entry gate failed. Stop here: the conditional three-day arithmetic implementation
is not started. No substitution, second cohort, DSL expansion or automatic
extension follows. Had it passed, implementation would still have required a
separate authorization; this unit could only report its proposed contents.

The [census record](census/REPORT.md) is structural coverage over a supplied local
export and unadjudicated lint/printed-text prevalence. It is separate from
byte-exact recompilation and provides no discovery credit. Preserve the author's
incorrect headline and correct its interpretation instead of presenting it as an
expressiveness measurement.

Both originally untracked examples are retained after revision: `mainnet_scan`
is a strict local export reader with input digest, distinctness checks and honest
metric labels; `real_property_probe` is a generic parser-only recorder with real
source digest verification and explicit errors. Original bytes are archived in
the census directory for historical provenance. Neither example is a new search
engine or a consensus/property evaluator. No dependency was added.

[Verification](consolidation-verification/verification.json) records real output
for fmt, workspace clippy with warnings denied, Python gate tests, release
workspace tests, `--through-completed` and `--ci`. The separate preservation audit
compares against the starting forge revision without modifying Git. Changes are
left uncommitted.
