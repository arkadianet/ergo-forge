# D00 — frozen property inventory and existing API measurement

Only D00 is registered and implemented. The full D00–D04 proposed queue remains
in [discovery section 5.3](../superpowers/specs/2026-09-11-discovery-capability-design.md#53-runner-compatible-proposed-registrations).
CI cannot enforce gates for unregistered units; this is the explicitly accepted
scheduling cost. Its acceptance semantics are unchanged. M05 remains stopped.
Capability 1 may be the endpoint; no later evaluator, linked-trace checker,
lifecycle constructor, solver or product endpoint has been implemented.

The [governing decision](D00-DECISION.md) and ROADMAP section 4 authorize the
narrow M00 projection extension. M00's original manifest bytes retain SHA-256
`7f956f559b3912348f6759ab0e1855cd90056fcbf94fe47c2305fb668805c53f`, and its original
`protectedPolicySha256` remains
`41729240759244bd3edc833e31359bfe50d8430f0b5a2e27ebc727706363cd10`.
The anchor projects original P units and original resolution entries. Separate
byte-pinned authentication requires every original P/M registration and P05
resolution unchanged, and allows only the exact D00 registration and resolution
plus corresponding completion advancement. Whole-object equality rejects every
other protected change. The actual checker is exercised with type-preserving
field mutations, missing/duplicate/reordered registrations and resolutions,
extra fields, D01 insertion, M05 resolution and a wrong anchor. These checks can
fail and do not merely compare an expected value to itself.

The historical D00 stop is resolved by the existing policy-pinned mechanism,
with only resolution metadata added to its original record. Both blockers and
all evidence remain in the [unchanged historical report](D00-STOP.md). The first
was the author's contradictory registration instruction; the second was a real
latent M00 projection defect. Neither is erased or relabelled as a product test.
No M00 rehash, pinned baseline edit or CI exception was used.

The [manifest](../../ergo-sandbox/tests/fixtures/properties/manifest.json) freezes
24 members: four families with two authored violations and two controls each,
plus all eight named boundaries. [Expected answers](../../ergo-sandbox/tests/fixtures/properties/expected.json)
are separate from execution inputs. They contain the roles, operands, intended
dispositions and explanations authored before inspecting execution output.
Exact source/tree bytes, canonical inputs, outputs, transactions, empty proofs,
contexts, token identities and hypothetical premises are pinned in each fixture.
Fixture-local `sourceId` selectors resolve to the pinned exact tree bytes;
`token:first-input-id` denotes the fixed canonical first-input box ID in that
registered request, not a proposed dynamic operator or inferred authority.
Node/compiler/Forge revisions, dirty status, lockfile and baseline hashes,
publication/exposure, exclusions and original caps are recorded in the manifest
and [registration receipt](registration-receipt.json). Manifest and answer byte
pins live outside their files in the test support; measured-output pins live in
the integration target. Missing, duplicate, renamed and altered cases, answers,
reference bytes and fabricated transfer/reviewer credit are rejected.

These are **authored expected answers, not evaluator results**. Synthetic
independent answer authorship means separation from a property implementation;
it is not independent human adjudication. No reviewer has been supplied, so
semantic measurement remains incomplete. The property specifications are frozen
design vocabulary inputs, not a shipped D01 schema or claim-producing API.
A later correction must use a new version outside this accepted score and keep
the original failed row.

A pre-lock serialization draft incorrectly used `funding` as an old extraction
role and omitted the explicit empty objective. API inspection identified both
before node/extraction measurement or external acceptance pins. The corrected
legacy declaration uses `attacker` for the author's funding input and
`objective.terms=[]`. The full [original draft](d00-draft-v0/manifest.json) and
serialization logs are retained. The serializer's initial “Frozen” message
preceded accepted harness pins; it did not record a scored inventory. No accepted
row, expected fact or measured verdict was changed. Re-running either example
against accepted output refuses to overwrite it.

The [measurement index](../../ergo-sandbox/tests/fixtures/properties/legacy-results.json)
links the byte-pinned [raw existing API measurement](../../ergo-sandbox/tests/fixtures/properties/legacy-results-raw.fixture),
which contains full node and extraction reports. All **35 reference transactions**
across the **24 rows** were individually accepted using the current
`evidence::validate::validate` API. Independent fresh extraction replay accounts
for another 35 validation calls: **70 total in the measurement capture**. The
inventory test additionally authenticates decoded input/output fields against
authored material and exercises refusal when required boxes are removed.
These extra verification calls are not discoveries or additional corpus rows.

Individual acceptance does **not** establish linked-trace replay. Even where
fixture bytes contain predecessor references, D00 has no cumulative trace,
UTXO-availability or environmental-linkage checker. D02 still owns that work.
Every measured reference remains `assumed-state`; no historical availability,
validated initialization or constructed reachability is claimed. The bounded
response answers describe intended finite promises, not validated trace claims.

Legacy extraction returned **2 confirmed violations / 35 transactions**, and
**33 accepted nonviolating / 35**. The two are `reserve_violation_2` and
`continuation_violation_1`; their raw accounting is preserved. Across unique
registered rows this is **2 / 24** with at least one extraction violation, not
new-property recall or discovery. The other six authored synthetic violation
rows have no legacy extraction violation. No new property producer exists:
evaluator agreement, applicable-guard measurement and new-property results are
`null`; new attributable discovery credit is **0** with eligible denominator
`null`. `holds-on-execution` is never “proved” or “safe”; a false author assertion
may be refuted without exposing a contract defect.

The [separate transfer registration](../../ergo-sandbox/tests/fixtures/properties/transfer-registration.json)
retains the planned **two cases**, denominator **null**, no accepted members and
no utility credit. Local sale/USE inventories were inspected; no defensible
independent non-extraction violating/control pair was supplied. The synthetic
24 are not renamed as transfer. Operator exposure and missing reviewer/per-case
effort measurements are explicit; no human hours are fabricated. Nomination was
limited to local review within its four-person-hour ceiling. Under the unchanged
design, this missing denominator requires cancellation of capabilities 2 and 3
at the checkpoint. D04 has not been registered, run or declared complete.

All work is uncommitted. No commit, push, PR comment, live acquisition, protocol
contact or broadcast occurred. Production source, legacy APIs, old corpus,
scoreboard, thresholds, caps and expected facts remain unchanged.

## Registered rows and measured extraction

The expected column is the frozen author answer, pending an evaluator and
independent semantic review. Reference acceptance is individual only.

| Case | Authored expectation | Accepted references | Legacy extraction violations |
|---|---|---:|---:|
| `reserve_violation_1` | `violated` | 1/1 | 0/1 |
| `reserve_violation_2` | `violated` | 1/1 | 1/1 |
| `reserve_control_1` | `holds-on-execution` | 1/1 | 0/1 |
| `reserve_control_2` | `holds-on-execution` | 1/1 | 0/1 |
| `issuance_violation_1` | `violated` | 1/1 | 0/1 |
| `issuance_violation_2` | `violated` | 1/1 | 0/1 |
| `issuance_control_1` | `holds-on-execution` | 1/1 | 0/1 |
| `issuance_control_2` | `holds-on-execution` | 1/1 | 0/1 |
| `continuation_violation_1` | `violated` | 1/1 | 1/1 |
| `continuation_violation_2` | `violated` | 1/1 | 0/1 |
| `continuation_control_1` | `holds-on-execution` | 1/1 | 0/1 |
| `continuation_control_2` | `holds-on-execution` | 1/1 | 0/1 |
| `bounded_response_violation_1` | `violated` | 3/3 | 0/3 |
| `bounded_response_violation_2` | `violated` | 3/3 | 0/3 |
| `bounded_response_control_1` | `holds-on-execution` | 3/3 | 0/3 |
| `bounded_response_control_2` | `holds-on-execution` | 3/3 | 0/3 |
| `missing_role` | `unresolved` | 1/1 | 0/1 |
| `ambiguous_role` | `unresolved` | 1/1 | 0/1 |
| `wrong_register_type` | `unresolved` | 1/1 | 0/1 |
| `arithmetic_overflow` | `unresolved` | 1/1 | 0/1 |
| `false_guard` | `not-applicable` | 1/1 | 0/1 |
| `short_response_horizon` | `unresolved` | 2/2 | 0/2 |
| `unbounded_eventuality` | `unsupported-property` | 3/3 | 0/3 |
| `unsupported_dynamic_expression` | `unsupported-property` | 1/1 | 0/1 |

## Requested verification — actual output

Every
command uses `CARGO_TARGET_DIR=./target-p00`; no external-only ignored test is
introduced or changed. The [verification receipt](d00-verification/verification.json)
records commands, UTC start times, actual exit codes and full-log SHA-256 hashes.


The initial full workspace run exited **101** at the frozen decompiler corpus:
placing raw node DTOs (with `ergoTree` keys) in `legacy-results.json` made recursive
discovery find new rows. This was a packaging error, not authorization to expand
the old corpus. The exact raw bytes and original measured-output SHA-256
`9a2031b9324ab62b9bb91f7cf6960aa8a00209148b1b2eca0c1dfbd6323ea451`
were moved to `legacy-results-raw.fixture`; the required JSON file is now a
reproducible index of the same statuses plus the raw file/hash. Manifest,
authored answers, transaction bytes, measured facts and original corpus remain
unchanged. The initial failure output is retained, and the final workspace run
below verifies the repaired packaging without modifying corpus acceptance.


| Command | Exit | Full output |
|---|---:|---|
| `cargo fmt --all -- --check` | **0** | [log](d00-verification/final/fmt.log) |
| `cargo clippy --workspace --all-targets -- -D warnings` | **0** | [log](d00-verification/final/clippy.log) |
| `cargo test -p ergo-sandbox --release --test property_inventory -- --show-output` | **0** | [log](d00-verification/final/property-inventory.log) |
| `cargo test --workspace --release` | **0** | [log](d00-verification/final/workspace.log) |
| `python3 -m unittest discover -s scripts -p test_roadmap_gate.py` | **0** | [log](d00-verification/final/python.log) |
| `python3 scripts/roadmap_gate.py --require D00` | **0** | [log](d00-verification/final/require-d00.log) |
| `python3 scripts/roadmap_gate.py --through-completed` | **0** | [log](d00-verification/through-completed.log) |
| `python3 scripts/roadmap_gate.py --ci` | **0** | [log](d00-verification/ci.log) |

Final workspace total: **485 passed / 0 failed / 1 ignored**, versus the
unchanged starting total of 481 / 0. The four added tests are the three D00
inventory tests and one M00 authentication regression. The unchanged external
node-checkout-only test remains ignored; required targets contain no ignored
tests. Python runner tests: **20 passed**. D00 direct target: **3 passed**.
The M00 gate reports **323 type-preserving field/member mutations rejected**, in
addition to its explicit structural and insertion controls.

Fmt produced no output. Clippy exited zero with `-D warnings`. `--require D00`
and `--through-completed` passed P00–P08, M00–M04 and D00. **`--ci` is 0 under
incremental registration**: D00 passed, M05 was accepted as a validated stop,
and D01–D04 were unregistered. It does not enforce gates for those future units.
The through/CI targets passed against the repaired packaging; affected fmt,
clippy, D00, workspace and direct-prefix verification were then rerun. The
[initial receipt](d00-verification/initial-verification.json) and
[initial workspace failure](d00-verification/workspace.log) remain available.

Actual final gate output (CI tail):

```text

D00: passed: required tests executed
scoreboard: passed
P00: passed: required tests executed
P01: passed: required tests executed
P02: passed: required tests executed
P03: passed: required tests executed
P04: passed: required tests executed
P05: passed: required tests executed
P06: passed: required tests executed
P07: passed: required tests executed
P08: passed: required tests executed
M00: passed: required tests executed
M01: passed: required tests executed
M02: passed: required tests executed
M03: passed: required tests executed
M04: passed: required tests executed
M05: stopped: recorded stop blocks this unit
D00: passed: required tests executed
report: docs/discovery/d00-verification/ci.json
```

[Preservation audit](d00-verification/preservation-audit.log): original pinned
artifacts and runner bytes match HEAD; prior stop objects, original policy
registrations/resolution and all stop evidence hashes match their retained data.
