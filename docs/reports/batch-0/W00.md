# W00 — adopt roadmap v2

The v2 spec governs the queue. The original ledger and P-series unit text are
preserved verbatim in `../ROADMAP-v1-queue.md`, with the later M-series records
copied there and retained in the roadmap. The three root reports moved under
`docs/reports/`; their incoming and outgoing Markdown links were repaired.

The v1 policy block is byte-identical to main. A separate v2 block registers
all eighteen new units at base `a57df087df85813c7f90a6f6f9ad6aaf5b4597cd`.
W00 is implemented; the other seventeen units await their gates. The two
completed prefixes are M04 and null. Original mapping manifests, protected
policy hash, metrics and measurement artifacts are unchanged.

The runner checks dependencies against both blocks, runs Python targets with
name discovery and execution evidence, and reports future v2 work as
`not-started` in CI. V1 gating and M05's stop retain their original semantics.
The scoreboard reads every original hash and compares measurements at the v2
base without changing the measurement revision or rerunning a benchmark.

Commands below use `CARGO_TARGET_DIR=/home/rkadias/coding/ergo-forge/target-p00`
for Cargo and gate execution. JSON reports include each subprocess command,
exit code, normalized output and output hash.

| Command | Exit | Evidence |
|---|---|---|
| `npm ci --prefix ui --cache /tmp/batch-0-npm-cache` | 0 | 20 packages installed |
| `python3 scripts/test_roadmap_gate.py` | 0 | [24 tests](w00-python-v2.log) |
| `cargo test -p ergo-sandbox --test mapping_inventory` | 0 | [All three tests](mapping-inventory.log) |
| `python3 scripts/roadmap_gate.py --require W00 --report docs/reports/batch-0/W00.json` | 0 | [Report](W00.json) |
| `python3 scripts/roadmap_gate.py --through-completed --report docs/reports/batch-0/w00-through-completed.json` | 0 | [Report](w00-through-completed.json), [transcript](w00-through-completed.log) |
| `python3 scripts/roadmap_gate.py --scoreboard-only --report docs/reports/batch-0/w00-scoreboard.json` | 0 | [Report](w00-scoreboard.json) |
| `python3 scripts/roadmap_gate.py --ci --report docs/reports/batch-0/w00-ci.json` | 0 | [Report](w00-ci.json), [transcript](w00-ci.log); M05 stopped, future v2 units not started |
| V1 block byte comparison against `git show main:docs/ROADMAP.md` | 0 | Exact match |
| `git diff --check` | 0 | No whitespace errors |

Two initial failed gate attempts are retained: [W00](W00-initial-failed.json)
returned 1 because an intentionally skipped inner self-test leaked its output;
[test output capture now isolates it](../../../scripts/test_roadmap_gate.py).
The [initial completed-prefix run](w00-through-completed-initial-failed.json)
returned 1 because the first approach changed the hash-pinned metrics document.
Both the metrics and the protected v1 policy were restored; the separate v2
block follows the author's corrected design. The final runs above passed.
